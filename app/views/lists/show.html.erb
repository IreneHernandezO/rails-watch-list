<h1> <%=@list.name %>  </h1>

<div class="container">
<div class="row">
<div class="col">
<%= link_to "Add movie(bookmark)", new_list_bookmark_path(@list) %>
<%= link_to "Back to my lists", root_path %>
</div>
</div>
</div>

<div class="container">
<div class="row">
<div class="col">
<%= @list.bookmarks.each do |bookmark| %>
<img src="<%= bookmark.movie.poster_url %>" alt="">
<%= link_to delete %>
</div>
</div>
</div>
