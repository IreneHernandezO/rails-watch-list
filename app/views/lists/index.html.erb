 <h1>My lists </h1>

 <ul>
    <% @lists.each do |list| %>
      <li>
        <%= link_to "#{list.name}", list_path(list) %>
      </li>
    <% end %>
 </ul>

   <div class= "btn btn-success">

    <%= link_to "Add a list", new_list_path %>

  </div>
